name: Fieldwork MCP secret redaction repair

on:
  push:
    branches:
      - research/mcp-secret-redaction-current

permissions:
  contents: write

jobs:
  repair:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: research/mcp-secret-redaction-current
          fetch-depth: 0
      - name: Apply exact redaction repair
        run: |
          python3 - <<'PY'
          from pathlib import Path
          path = Path('packages/playwright-core/src/tools/backend/context.ts')
          text = path.read_text()
          old = '''  redactSecrets(text: string): string {\n    for (const [secretName, secretValue] of Object.entries(this.config.secrets ?? {})) {\n      if (!secretValue)\n        continue;\n      text = text.replaceAll(secretValue, `<secret>${secretName}</secret>`);\n    }\n    return text;\n  }'''
          new = '''  redactSecrets(text: string): string {\n    const replacements = new Map<string, string>();\n    for (const [secretName, secretValue] of Object.entries(this.config.secrets ?? {})) {\n      if (!secretValue)\n        continue;\n      const marker = `<secret>${secretName}</secret>`;\n      if (!replacements.has(secretValue))\n        replacements.set(secretValue, marker);\n      const serializedValue = JSON.stringify(secretValue).slice(1, -1);\n      if (!replacements.has(serializedValue))\n        replacements.set(serializedValue, marker);\n    }\n\n    const candidates = [...replacements].sort(([a], [b]) => b.length - a.length);\n    let result = '';\n    for (let i = 0; i < text.length;) {\n      const replacement = candidates.find(([value]) => text.startsWith(value, i));\n      if (!replacement) {\n        result += text[i++];\n        continue;\n      }\n      result += replacement[1];\n      i += replacement[0].length;\n    }\n    return result;\n  }'''
          count = text.count(old)
          if count != 1:
              raise SystemExit(f'expected exactly one redactSecrets body, found {count}')
          path.write_text(text.replace(old, new))
          PY
          git diff --check
          git diff -- packages/playwright-core/src/tools/backend/context.ts
      - name: Install and build
        run: |
          npm ci
          npm run build
          npx playwright install chromium
      - name: Run focused secret controls
        run: npx playwright test -c tests/mcp/playwright.config.ts --project=chromium tests/mcp/secrets.spec.ts tests/mcp/secrets-redaction-research.spec.ts
      - name: Run focused lint
        run: npx eslint packages/playwright-core/src/tools/backend/context.ts tests/mcp/secrets-redaction-research.spec.ts
      - name: Publish repaired source and remove carrier
        run: |
          rm .github/workflows/fieldwork-mcp-secret-redaction-repair.yml
          git add packages/playwright-core/src/tools/backend/context.ts .github/workflows/fieldwork-mcp-secret-redaction-repair.yml
          test "$(git diff --cached --name-only | sort)" = $'.github/workflows/fieldwork-mcp-secret-redaction-repair.yml\npackages/playwright-core/src/tools/backend/context.ts'
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git commit -m 'fix: redact serialized and overlapping MCP secrets'
          git push origin HEAD:research/mcp-secret-redaction-current
